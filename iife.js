// An IIFE (Immediately Invoked Function Expression) is a function in JavaScript that is executed immediately after it is defined. It doesn’t require an explicit call to run—it’s
//  invoked as soon as the interpreter encounters it.

(function chai()
    console.log("IIFE executed immdiately");
})();

(
   name{
        console.log('DB connected Two');
    }

)()